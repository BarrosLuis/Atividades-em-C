#include <stdio.h>
#include <stdlib.h>

/* Problema: Imprimir na tela os números de 1 a 10, usando estrutura de repetição
    while em C */

int main() {
int contador=1;
while(contador<=10){
    printf("%d\n", contador);
    contador++;
}
return 0;
}
